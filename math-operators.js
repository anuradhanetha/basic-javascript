const RADIUS = 5;

let area = Math.PI * (Math.pow(RADIUS, 2)); // using Math Pow 
// area = Math.PI * RADIUS * RADIUS;//
console.log(area);

let a = 5;
let b = 10;
let c = 30;

let average = (a + b + c )/ 3;
console.log(average);